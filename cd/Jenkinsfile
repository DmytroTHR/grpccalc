def version = "1.0.${env.BUILD_NUMBER}"

pipeline{
    agent any

    stages{
        stage('Helm linter'){
            steps{
                sh 'helm lint /helm/client'
                sh 'helm lint /helm/server'
            }
        }
    }
}